/**
 * Minimal stateless page component that renders the demo UI.
 *
 * Renders a single <div> containing the text "Demo". Exported as the default component for the /demo route.
 *
 * @returns The JSX element for the demo page.
 */
export default function Demo() {
  return <div>Demo</div>;
}